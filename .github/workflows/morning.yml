name: 1-Morning-Data-Job

on:
  schedule:
    - cron: '0 23 * * *' # 日本時間 08:00 (世界標準時 23:00)
  workflow_dispatch: # 手動実行ボタン

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          pip install pandas selenium webdriver-manager pytz

      - name: Run Morning Job
        run: python morning_job.py
        env:
          TZ: 'Asia/Tokyo'

      - name: Commit and Push Base CSVs
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          # 【修正】削除されたファイルも反映させるため -A を追加
          git add -A data/*.csv
          # 変更がない場合でもエラーにせず終了する設定
          git commit -m "Cleanup old data and Create new base [$(date)]" || exit 0
          git push
